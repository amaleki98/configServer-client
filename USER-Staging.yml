my:
  text: "hello; **** This is USER Staging Environment *****"

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mysql_maindb_Staging
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQL8Dialect
      show-sql: false
    properties:
      hibernate.format_sql: false
      hibernate.generate_statistics: true 
  
